{
  "name": "moot-space",
  "private": true,
  "version": "0.0.0",
  "packageManager": "npm@11.12.1",
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --filter=@moot-space/web",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test:unit": "turbo run test:unit"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
